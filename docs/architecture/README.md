# Architecture

This folder documents the Enterprise Orchestrator Appliance architecture.

Read these first:

- `overview.md`: system boundary, major components, and request flow.
- `data-flow.md`: request, evidence, model, and audit data movement.
- `deployment.md`: deployment targets and appliance principles.

Future decisions should be recorded under `decisions/` when implementation
choices become durable architecture.
