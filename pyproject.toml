[project]
name = "enterprise-orchestrator-appliance"
version = "0.2.0"
description = "EAAP-aligned operator console and local LLM appliance foundation."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
api = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.30,<1",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
